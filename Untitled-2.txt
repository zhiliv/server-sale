    Соединение = Новый HTTPСоединение("46.72.31.213",81);
    HTTPЗапрос = Новый HTTPЗапрос("/getClient1C/"+Строка(месяц)+Строка(день)); 
    HTTPЗапрос.Заголовки.Вставить("Content-type", "application/x-www-form-urlencoded;charset=utf-8");
    HTTPЗапрос.УстановитьТелоИзСтроки("data="+ТекстJSON,КодировкаТекста.UTF8,ИспользованиеByteOrderMark.НеИспользовать);
    ИмяФайлаОтвета = ПолучитьИмяВременногоФайла("txt"); 
    Соединение.ОтправитьДляОбработки(HTTPЗапрос, ИмяФайлаОтвета);   
    ФайлОтвета = Новый Файл(ИмяФайлаОтвета);
	
